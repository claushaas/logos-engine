// Purpose: Prompt public API.
// What it should do: Re-export prompt builders.
// Why it exists: Creates one import boundary for semantic prompt functions.

export function index() {
  // TODO: return deterministic LLM messages for this semantic task.
  return [];
}
