// Purpose: Prompt builder for document generation.
// What it should do: Build deterministic messages to generate Markdown from canonical answers and templates.
// Why it exists: Ensures documents derive from structured source material.

export function generate_document() {
  // TODO: return deterministic LLM messages for this semantic task.
  return [];
}
