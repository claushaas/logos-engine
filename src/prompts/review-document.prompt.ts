// Purpose: Prompt builder for document review.
// What it should do: Build prompts for minimal, traceable document improvements.
// Why it exists: Prevents broad rewrites when only targeted patches are needed.

export function review_document() {
  // TODO: return deterministic LLM messages for this semantic task.
  return [];
}
