// Purpose: Project filesystem abstraction.
// What it should do: Expose safe read/write/list operations scoped to project root.
// Why it exists: Keeps core logic independent from raw fs calls.

export const project_fs = {
  // TODO: implement project filesystem abstraction.
};
