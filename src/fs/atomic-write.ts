// Purpose: Atomic write helper.
// What it should do: Write files through temporary files and rename operations.
// Why it exists: Prevents partial/corrupt writes when generating docs.

export const atomic_write = {
  // TODO: implement atomic write helper.
};
