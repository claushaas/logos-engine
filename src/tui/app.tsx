// Purpose: Root TUI component.
// What it should do: Mount the app frame, route between screens, and bind global keyboard actions.
// Why it exists: The TUI is the main MVP surface for structured documentation work, not a generic chat UI.

import React from "react";
import { Text } from "ink";

export function LogosTuiApp(): JSX.Element {
  return <Text>LOGOS Engine TUI — TODO</Text>;
}
