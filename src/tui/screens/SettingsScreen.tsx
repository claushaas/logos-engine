// Purpose: Settings screen.
// What it should do: Show local provider/model/configuration values.
// Why it exists: Allows local-first configuration without hiding operational assumptions.

import React from "react";
import { Text } from "ink";

export function Settingsscreen(): JSX.Element {
  return <Text>Settingsscreen — TODO</Text>;
}
