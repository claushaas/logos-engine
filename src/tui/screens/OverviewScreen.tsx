// Purpose: Project overview screen.
// What it should do: Display phase completeness, diagnostics, artifacts, and next recommended action.
// Why it exists: Gives the user immediate structural awareness before acting.

import React from "react";
import { Text } from "ink";

export function Overviewscreen(): JSX.Element {
  return <Text>Overviewscreen — TODO</Text>;
}
