// Purpose: Interview screen.
// What it should do: Display the current question, guidance, answer input, and assessment state.
// Why it exists: Supports one-question-at-a-time lifecycle control.

import React from "react";
import { Text } from "ink";

export function Interviewscreen(): JSX.Element {
  return <Text>Interviewscreen — TODO</Text>;
}
