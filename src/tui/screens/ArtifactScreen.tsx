// Purpose: Artifact screen.
// What it should do: List generated HTML, Markdown, reports, and Agent Packs.
// Why it exists: Makes outputs inspectable and regenerable.

import React from "react";
import { Text } from "ink";

export function Artifactscreen(): JSX.Element {
  return <Text>Artifactscreen — TODO</Text>;
}
