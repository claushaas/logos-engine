// Purpose: Phase workspace screen.
// What it should do: Display documents for one phase, missing docs, diagnostics, and contextual actions.
// Why it exists: Lets the user operate one normative axis at a time.

import React from "react";
import { Text } from "ink";

export function Phasescreen(): JSX.Element {
  return <Text>Phasescreen — TODO</Text>;
}
