// Purpose: TUI state model.
// What it should do: Define selected screen, phase, document, diagnostics, and active operation state.
// Why it exists: Separates render state from core business logic.

export type TuiState = {
  // TODO: define the shape needed by the TUI layer.
};
