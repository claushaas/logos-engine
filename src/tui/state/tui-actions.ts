// Purpose: TUI action helpers.
// What it should do: Define state transitions triggered by user interaction.
// Why it exists: Keeps keyboard/UI logic explicit and testable.

export type TuiActions = {
  // TODO: define the shape needed by the TUI layer.
};
