// Purpose: TUI frame component.
// What it should do: Render global header, footer, and active screen layout.
// Why it exists: Gives consistent navigation and status context.

import React from "react";
import { Text } from "ink";

export function Appframe(): JSX.Element {
  return <Text>Appframe — TODO</Text>;
}
