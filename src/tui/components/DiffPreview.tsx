// Purpose: Diff preview component.
// What it should do: Render proposed document patches before acceptance.
// Why it exists: Prevents silent overwrites of canonical docs.

import React from "react";
import { Text } from "ink";

export function Diffpreview(): JSX.Element {
  return <Text>Diffpreview — TODO</Text>;
}
