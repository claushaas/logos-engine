// Purpose: Activity log component.
// What it should do: Render events such as LLM calls, state transitions, and writes.
// Why it exists: Improves auditability and debugging.

import React from "react";
import { Text } from "ink";

export function Activitylog(): JSX.Element {
  return <Text>Activitylog — TODO</Text>;
}
