// Purpose: Validation panel component.
// What it should do: Render schema/document diagnostics.
// Why it exists: Makes blockers explicit before generation/export.

import React from "react";
import { Text } from "ink";

export function Validationpanel(): JSX.Element {
  return <Text>Validationpanel — TODO</Text>;
}
