// Purpose: Agent console component.
// What it should do: Render bounded semantic interaction with the LLM workflow.
// Why it exists: Provides conversational control without making chat the whole product.

import React from "react";
import { Text } from "ink";

export function Agentconsole(): JSX.Element {
  return <Text>Agentconsole — TODO</Text>;
}
