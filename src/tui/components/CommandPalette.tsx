// Purpose: Command palette component.
// What it should do: Offer keyboard-driven actions for current context.
// Why it exists: Keeps the TUI fast without requiring memorized shortcuts.

import React from "react";
import { Text } from "ink";

export function Commandpalette(): JSX.Element {
  return <Text>Commandpalette — TODO</Text>;
}
