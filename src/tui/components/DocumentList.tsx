// Purpose: Document list component.
// What it should do: Render documents in a phase with status markers.
// Why it exists: Lets users select what to generate, review, or validate.

import React from "react";
import { Text } from "ink";

export function Documentlist(): JSX.Element {
  return <Text>Documentlist — TODO</Text>;
}
