// Purpose: Artifact list component.
// What it should do: Render generated output files and freshness status.
// Why it exists: Keeps derived artifacts visible and traceable.

import React from "react";
import { Text } from "ink";

export function Artifactlist(): JSX.Element {
  return <Text>Artifactlist — TODO</Text>;
}
