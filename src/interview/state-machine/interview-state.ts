// Purpose: Interview state definitions.
// What it should do: Define all lifecycle states such as asking, assessing, complementing, finalizing, and generating.
// Why it exists: The agent lifecycle must be explicit, not prompt-dependent.

export const interview_state = {
  // TODO: implement interview state definitions.
};
