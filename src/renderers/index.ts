// Purpose: Renderer public API.
// What it should do: Re-export Markdown, HTML, and Agent Pack renderers.
// Why it exists: Creates a boundary for output generation.

export const index = {
  // TODO: implement renderer public api.
};
