// Purpose: Agent review prompt renderer.
// What it should do: Render review tasks into deterministic review prompts.
// Why it exists: Supports agent-based quality gates.

export const render_review_prompt = {
  // TODO: implement agent review prompt renderer.
};
