/**
 * LOGOS Core — Configuration module.
 *
 * Owns project configuration logic and config schema.
 * Boundary: must not import Pi, Ink, React, TUI, or CLI modules.
 */

export type CoreConfigModule = 'core.config';
