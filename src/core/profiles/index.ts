/**
 * LOGOS Core — Profiles module.
 *
 * Owns profile resolution, loading, and contract validation.
 * Boundary: must not import Pi, Ink, React, TUI, or CLI modules.
 */

export type CoreProfilesModule = 'core.profiles';
