// Purpose: Phase dependency rules.
// What it should do: Describe which phases/documents depend on earlier outputs.
// Why it exists: Prevents downstream generation from ignoring upstream gaps.

export const phase_dependencies = {
  // TODO: implement phase dependency rules.
};
