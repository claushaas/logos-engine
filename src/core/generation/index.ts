/**
 * LOGOS Core — Generation module.
 *
 * Owns generation preflight, canonical document compilation, and artifact planning.
 * Boundary: must not import Pi, Ink, React, TUI, or CLI modules.
 */

export type CoreGenerationModule = 'core.generation';
