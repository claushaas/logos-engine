// Purpose: HTML export adapter.
// What it should do: Map Executive JSON into HTML render inputs.
// Why it exists: Keeps HTML derived from validated exchange state.

export const html_adapter = {
  // TODO: implement html export adapter.
};
