// Purpose: Shared public API.
// What it should do: Re-export common types, errors, and utilities.
// Why it exists: Creates a single import boundary for cross-cutting code.

export const index = {
  // TODO: implement shared public api.
};
